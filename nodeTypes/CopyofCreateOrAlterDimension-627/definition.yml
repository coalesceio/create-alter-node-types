fileVersion: 1
id: "627"
isDisabled: false
metadata:
  defaultStorageLocation: null
  error: null
  nodeMetadataSpec: |
    capitalized: Create or Alter DIM Table
    short: CDIM
    plural: Dimensions
    tagColor: '#333BA6'

    deployStrategy: advanced

    config:
    - groupName: Create Table Options
      items:
      - type: materializationSelector
        default: table
        options:
        - table
        - transient table
        isRequired: true
        
      - displayName: '{{node.parameters.dsmp}}'
        type: dropdownSelector
        attributeName: tsk
        default:  "standard"
        options:
        - "{{sample()}}"
        isRequired: false
name: Copy of Create Or Alter Dimension
type: NodeType
